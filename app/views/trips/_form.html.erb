<%= simple_form_for [@user, @trip] do |f| %>
  <%= f.input :destination %>
  <%= f.input :price_per_day %>
  <%= f.input :min_duration %>
  <%= f.association :shuttle %>
  <%= f.association :spatioport %>
  <%= f.input :photo, as: :file %>
  <%= f.submit %>
<% end %>
